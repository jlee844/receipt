[project]
name = "agent-receipt"
version = "0.1.0"
description = "What did that AI coding session actually do, and what did it cost?"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
receipt = "receipt.__main__:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
